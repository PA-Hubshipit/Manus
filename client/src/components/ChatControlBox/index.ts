/**
 * ChatControlBox Component Exports
 * 
 * This is the main entry point for the ChatControlBox reusable component.
 * Import from this file to get the component and all related types.
 * 
 * @example
 * import { ChatControlBox, type ChatControlBoxProps, type Message } from '@/components/ChatControlBox';
 */

export { ChatControlBox, default } from './ChatControlBox';
export type { 
  ChatControlBoxProps, 
  Message, 
  Attachment, 
  SavedConversation 
} from './ChatControlBox';
